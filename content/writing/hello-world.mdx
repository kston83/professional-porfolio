---
title: "Hello, world"
description: "A placeholder post proving the MDX content pipeline works end to end."
date: "2026-08-20"
tags: ["meta"]
---

This is a placeholder post. It exists to prove the content pipeline —
frontmatter parsing, reading-time estimate, and MDX rendering through the
site's type system — works end to end.

The real first post will cover how this site's shell was designed and
built, ported over from a fantasy football dashboard's design system.

## A heading, for scale

Body copy renders in Instrument Sans at the body step. `Inline code` renders
in IBM Plex Mono. Headings render in Fraunces, matching the rest of the
site's display type.

- List items
- Render with hanging markers
- Not boxes
